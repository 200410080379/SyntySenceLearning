# 协作约定

本仓库用于长期学习 Synty 场景搭建。优先完成用户当前指定的研究或训练，并把可复用的结论整理到对应资源包目录。

## 组织方式

- 新资源包使用 `packs/<pack-id>/`，每项训练使用 `studies/<study-id>/`；名称使用小写字母、数字和连字符。
- 每项训练至少说明目标、资源包与 UE 版本、原场景观察、模型接口、布局、复现方法、验证结果和未覆盖范围。可使用 `templates/study/`。
- 跨资源包通用方法写入 `docs/`；具体模型名称、材质引用和摆放配方保留在所属资源包训练中。
- 脚本从自身位置、工程信息或显式参数解析路径，不写入个人机器路径，不依赖 JSQS 工程。输出的 UE 学习资产使用 `/Game/SyntySenceLearning/<Pack>/<Study>` 下的独立目录。

## 研究与验证

- 先观察原包场景，再测量真实模型的枢轴、尺寸、旋转、边界轮廓和材质。不要仅凭模型名称或包围盒推断接口兼容。
- 先验证最小组合，再扩大布局；同时检查近景几何、远景显示和保存重开后的实例。
- 结论必须能对应到实际布局、脚本输出或 UE 截图。禁止硬编码“通过”、编造测试结果、只用 Actor Z 相等代替接缝测量，或先自动修正再冒充只读验收。
- 保留失败原因和修正依据。历史结果明确标注测试日期、资源版本和适用范围；新环境另行运行验证。
- 几何接缝、材质显示、碰撞与导航是不同检查。只报告实际完成的项目。
- 用户要求使用现有资源时，不用引擎基础体或生成网格替代资源包模型来掩盖拼接问题。

## 源资产和仓库内容

- 保留源资产及源 Demo；研究时采用只读测量或独立学习关卡。临时隐藏原场景对象后恢复可见性，不保存研究产生的源地图改动。
- 需要材质修正时，在学习目录创建派生材质并记录作用范围，不覆盖资源包原材质或原贴图。
- 不提交原始或派生的 `.uasset` / `.umap`、完整几何导出、原贴图、完整源 Demo 布局、缓存、日志、凭据和其他私有数据。检查新增文件内容，不能仅依赖扩展名与 `.gitignore`。
- 布局可保存模型引用、局部学习样板的实例变换和自创场景配方；几何测量数据需要本地从使用者已有的资源重新生成。
- 不复制依赖环境、MCP 服务或个人配置入库。沿用已有可用的 UE Python / MCP 执行途径。
- 不为第三方资源添加许可证授权声明；资源使用以其已有授权为准。

## 本地环境与外部操作

- Windows 上，任务工程、下载与构建暂存优先放 F 盘；可配置工具缓存优先使用 `F:\DevCache\<tool>`，先检查工具实际支持的设置。避免在 D 盘创建新缓存。
- 保留已有 F 盘 Android、Gradle、Epic 和 UE 缓存路径。不要把凭据、聊天历史、依赖环境或系统目录当缓存搬迁。
- 保持已有代理、DNS 和网络路由不变。不要为训练工具擅自修改全局渲染设置或缓存配置。
- 外部写入按用户已授权范围执行，不增加多余确认流程。未经明确授权，不向他人发送邮件、聊天消息或其他外部消息。
