# <训练标题>

将本模板复制到 `packs/<pack-id>/studies/<study-id>/README.md`，删除提示文字并填写实际内容。`study.json` 是训练元数据起点；未测量的值保留为空，不填示例通过结果。

填写 `study.json` 的 `id`、`pack`、输出 `map`、`layout` 和 `scripts`；脚本字段使用“角色 → 本训练相对路径”的对象，例如 `"build": "Scripts/build_study.py"`。在所属 `pack.json` 的 `studies` 中登记 ID、可选 alias 和 manifest 相对路径。新资源包还需在根目录 `catalog.json` 登记其 `pack.json`，并在包文件中列明依赖资源及编辑器插件。不要把模板中的占位符注册为可执行训练。

## 目标与依赖

说明本次要解决的问题、资源包版本、UE 版本、本地资产引用根目录和原场景。列明需要用户已有的资源，不把资源本体打包进训练。

## 原场景观察

说明观察区域、模块分工、尺寸、枢轴、变换和材质。每项结论关联实际证据，并限定适用范围。若临时隐藏了建筑，说明完整场景中哪些地面原本被遮挡。

## 最小组合与独立布局

给出模型引用、位置、朝向和缩放，说明兼容边界。记录拆开对照、转角与入口样板，再说明扩大布局的规则。布局文件放在 `Data/`；不要附带源 Demo 全布局或完整网格几何。

## 复现

填写已实际验证的准备命令、UE Python 执行入口、依赖资产、输出关卡路径、目标已存在时的行为，以及修改后的同步和只读检查方式。避免个人机器绝对路径。

## 验证与问题记录

| 检查 | 方法与阈值 | 实际结果 | 证据 |
| --- | --- | --- | --- |
| 地面覆盖 | 待填写 | 未运行 | 待填写 |
| 接缝几何 | 待填写 | 未运行 | 待填写 |
| 近远景显示 | 待填写 | 未运行 | 待填写 |
| 保存重开 | 待填写 | 未运行 | 待填写 |

写明失败实例和修正依据。当前复现与历史报告分开，未运行的碰撞、导航或性能检查不能写成通过。

## 截图与文件

截图放在 `Screenshots/`，使用相对链接，说明观察点和用途。代码放 `Scripts/`，布局与精简报告放 `Data/`。完整资源、几何、日志与缓存留在本地。

## 可以复用的结论

写出已被实测支持的搭建规则，以及还需要进一步验证的问题。
